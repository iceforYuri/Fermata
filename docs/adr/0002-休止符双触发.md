# 休止符采用双触发：时间环走满或连续工作超阈值

时间环属于单次任务会话（切换任务即重置），只管单任务的专注节律；但频繁切换者的环永远走不满，"该休息了"的职责会丢失。因此休止符的触发条件定为：时间环走满，或连续无休息工作超过阈值（默认 90 分钟，设置可调）。

## Consequences

- 事件日志需额外记录"连续工作段"，统计页多一个字段。
- 用户感知几乎无变化：环的读数含义不变，只是休息保证更可靠。
