# Fermata —— 日常版面与任务调度

桌面应用：把一天排成一页版面，管理并行工作的挂起与恢复，度量专注时间。

## Language

**进程**：
当天的一件进行中的工作，有状态（挂起/运行/完成），在版面中占一行。
_Avoid_: 任务、事项（口语可用，文档与界面统一用"进程"）

**挂起**：
进程的默认状态：已排入版面，但当前不在做。
_Avoid_: 暂停（暂停是运行中的计时停止，不是状态切换）

**运行**：
唯一位于折线上的进程状态，计时进行中。

**断点**：
进程的"做到哪了"一句话；挂起时显示在进程行的导语槽。断点 = 步骤栈的栈顶条目；写断点 = 往栈顶压一条断点条。
_Avoid_: 备注、记录

**步骤栈**：
进程的步骤清单：新条目录入置顶，旧条目往下挤。栈内两种条目——**步骤**（可勾选完成，勾选 = 原位划线变淡不沉底）与**断点条**（切走/改断点时压入的便签，不可勾选，行首 ▸）。条目均可就地改文、删除。

**当前步骤**：
进程行的导语槽：显示步骤栈的栈顶条目（步骤或断点条）；栈空时为手写文字；挂起时即断点。

**老化**：
本次挂起的持续时长：距上一次挂上至今，捞回即清零重计（一天内反复捞放只反映最近这段）；等AI 是挂起子状态，时段照算（等AI 只是不参与呈现）。

**折线**：
版面的水平分界：上方为已完，折线上为运行，下方为挂起。

**稿库**：
计划池——今日剩余与明日草稿，尚未排入版面。
_Avoid_: 待办清单、计划列表

**计划**：
已写下但未排入版面的事项；拖入版面即成为进程。

**预定日**：
计划的目标日期；到了那天，计划进入进程页左栏的"今日剩余"组。

**时间片**：
一次预期专注的时长，可设置；走满触发休止符。
_Avoid_: 番茄钟（时间片属于单个进程会话，语义不同）

**时间格**：
统计页日网格的 10 分钟基本单位；18 列 × 6 行共 108 格，覆盖 06:00–24:00 时窗。悬停圆点显示该时间格内的占用起止（钳制在格窗内）。
_Avoid_: 时间片（那是单次会话的预期专注时长，两词严格不混用）

**时间环**：
运行中进程的时间片倒计时环，属于单次任务会话，切换即重置。

**色标**：
进程的自由分类标记，从七种颜色中选取；语义由用户自定义，产品不预设级别。
_Avoid_: 重要度（已不再是固定级别）、标签

**等 AI**：
挂起的子状态：进程在等待外部工作（如 AI 任务）完成，不是被遗忘，不参与老化呈现（行渐褪与「挂 xx」加深都与它无关）。

**休止符**：
休息/切换提醒；触发条件为时间环走满或连续工作超阈值。

**休息态**：
计时暂停的状态：休止符触发后进入，休止符弹窗与进程页的休息页是同一状态的两种渲染；继续由用户显式开启。

**累计用时**：
当天投在单个进程上的总时长（显示为 xh xm）。

**付印**：
一天的收尾动作：版面归档并可导出。

**完成档案**：
收纳已完成进程的子页面，支持重新打开。

**回归**：
把进程变成计划：在目标日稿库生成一条同名计划（仅今天/未来）；源进程原地封存不动，历史不改写。计划拖到版面即成为新进程。
