{
  "name": "fermata",
  "private": true,
  "version": "1.4.0",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "seed": "cd src-tauri && cargo run --bin seed",
    "seed:deep": "cd src-tauri && cargo run --bin seed -- --deep",
    "verify:m0": "cd src-tauri && cargo run --bin verify_m0"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "~5.6.3",
    "vite": "^5.4.11",
    "@tauri-apps/cli": "^2.0.0",
    "playwright": "^1.48.2",
    "pngjs": "^7.0.0"
  }
}
