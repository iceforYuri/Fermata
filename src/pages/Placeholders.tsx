export function StatsPage() {
  return (
    <div className="placeholder-page" data-testid="stats-page">
      统计 · M3 排期（三视角 / 日网格 / 计划日期模型）
    </div>
  );
}

export function SettingsPage() {
  return (
    <div className="placeholder-page" data-testid="settings-page">
      设置 · M4 排期（时间片 / 连轴转阈值 / 热键 / 置顶 / 主题）
    </div>
  );
}
