# Fermata 开发守则（AGENTS.md）

> 本文件是常驻规则。每次开发会话开始先读本文件，再按任务需要读 docs/。人类已敲定的部分是合同；质感层是你的创作区。

## 使命

Fermata 是一个 Windows 桌面效率工具：把一天排成一页版面，管理并行工作的挂起与恢复，度量专注时间。内核是任务调度（进程状态机 + 断点 + 计时 + 休止符），报纸式排版是这层调度数据的呈现。用户是全天与 AI 协作的知识工作者——频繁切换、容易忘记上一件事做到哪。

## 设计宪法（不可违反，每个阶段交付前自查）

1. **调度为内核，排版为呈现**：行为正确永远优先于视觉好看（ADR-0001）
2. **时间的默认值是"不计"**：休止符无超时、✕=进入休息、恢复必须显式（ADR-0002：环走满 或 连续工作 90 分钟双触发）
3. **颜色只属于色标**：无全局强调色；色标 7 色、语义用户自定义；层级靠字号/字重/透明度，不靠色块
4. **玻璃语言边界**：backdrop-filter 只盖自家内容（休息页、完成档案）；对外浮层（休止符弹窗、切换浮层）是实心暖卡
5. **全 app 无表单**：一切编辑是"排版文字的原地变形"，1px 下划线是唯一编辑指示
6. **推拉不弹窗**：面板是版面重排，主窗内禁一切模态
7. **六原语薄接口**：前端不 import 任何 Tauri API；系统层收敛为 唤出/隐藏/置顶/焦点/空闲/热键 六个函数

## 词汇（必须使用，不许改叫）

见 CONTEXT.md。要点：叫**进程**不叫任务；**稿库**不是待办清单；**断点、当前步骤、老化、折线、休止符、色标、已完栏、完成档案、休息态**。UI 文案、代码标识符、git 提交信息一律用这套词。

## 文档地图（按此顺序读）

1. `CONTEXT.md` —— 词汇表
2. `docs/01-进程页.md` —— 主页面全部规格（含视觉规格）
3. `docs/02-统计页.md` —— 三视角 / 日网格 / 计划日期模型
4. `docs/04-系统层.md` —— 切换浮层 / 休止符弹窗 / 休息页
5. `docs/03-设置页.md` —— 分组与默认值
6. `docs/adr/0003-技术栈-tauri-react.md` + `docs/05-技术调研.md` —— 技术决策与兜底条款
7. 原始参考图（第三方 Gika 应用截图）已因版权移除；现行面貌以 `docs/screenshots/release/` 为准

## 技术纪律

- Tauri 2 + React + TypeScript + Vite；数据 SQLite（tauri-plugin-sql 或 Rust 侧 rusqlite）
- 窗口全程 **opaque**：不碰 `transparent: true`（绕开 Windows bug 群，见 05）；圆角交给 Win11 DWM
- 视觉规则全部做成 design tokens（`src/styles/tokens.css`，CSS 变量），与 01 视觉规格一一对应
- 不引 UI 组件库（避免表单气质）；CSS 手写；只用 baseline CSS 特性（WebView2 版本随用户机器漂移）
- 事件日志 append-only，从第一天记全（统计与未来 AI 批注的粮食）
- 环境：本机访问外网走代理 `http://127.0.0.1:7897`（npm/pnpm/cargo 网络失败时先配代理再重试）
- git：本目录为独立仓库；每完成一个子任务提交一次，提交信息用词汇表用语
- 分支模型（2026-09-20 用户定）：`dev` = 开发记录与合并主线；`main` = 发布分支，仅在用户完全确认后由 dev 合入；新功能从 dev 开 `feature/<名>` 分支，完成回归后合回 dev

## 视觉底座（已定，不可改）

亮·淡暖 #FCFBF7 / 暗·工作台 #26221E（v1.1 修订，原 #FAF7F2 偏黄）；色标暖调 7 色（具体色值在 M1 由你按审美方向定稿后写入 tokens）；字号阶梯 28/15/12.5/12；数字一律等宽；卡片 = 1px 色标边框 + 同色 12–15% 染底 + 4–6px 圆角；折线 1px；时间环 64px。细节见 docs/01 视觉规格一节。

## 质感层（你的创作区——拿出你最好的审美）

底座之上的这些都归你：动效曲线与编排、过渡与微交互、阴影与层次的克制使用、图标风格、空态文案气质、悬停与光标的细节打磨。方向见下一节。

## 审美方向（防土指南）

**基调**：克制的奢华——贵气来自留白、对齐与材质，不来自装饰；编辑部气质——像一本被认真排过的杂志，字号即层级；精密仪器——计时冷静、状态可读；暖而清醒——纸感的温度，配不苟言笑的排版纪律。

**动效**：物理感——spring / ease-out，元素有质量；连贯——推拉是版面重排的连续过程，不是开关；有因之动——动效解释结构（折叠 = 沉降、勾选 = 推进、玻璃 = 遮盖）；快而不催——120–260ms，快于用户预期但从不躁动；呼吸——时间环最后 5 分钟的缓慢呼吸是全 app 唯一的"活物"，其余一律克制。

**质感**：哑光；玻璃是"一层薄雾"，不是糖果；细节锐利——1px 边、等宽数字、像素级对齐。

**正面参照**：Linear 的克制、Things 3 的排版纪律、Vercel/Geist 的黑白纪律、Apple Liquid Glass 的材质（只在我们允许的位置）。

**禁（土味清单，越线即返工）**：渐变按钮与彩虹渐变；默认蓝 #3B82F6；emoji 当图标；到处发光与投影；pill 大圆角全家桶；玻璃越界（违宪）；卡片套卡片；花哨 loading 动画；紫色 SaaS 风。

## 工作协议

- 阶段顺序：**PoC → M0 数据内核 → M1 进程页 → M2 系统层 → M3 统计页 → M4 设置+双主题**
- 每阶段交付前自验：跑起来（`pnpm tauri dev`）、勾完该阶段验收清单、宪法七条自查、截图存入 `docs/screenshots/<阶段>/`
- UI 截图用 Playwright 对 Vite dev server 逐状态截取；**浮层组件做成路由可达**（如 `#/overlay/switcher`、`#/rest`）以便浏览器截图；原生行为（热键、焦点、空闲）以运行日志为证据
- 文档没有答案的问题：先查 docs/；仍无 → 用最保守方案实现并写入 `docs/deviation.md`（偏离必须留痕，禁止静默改设计）
- PoC 三项（不抢焦点弹窗 / 页内 backdrop-filter / 热键+空闲）任一失败：先修复；确属硬阻断 → 按 ADR-0003 兜底条款切 Electron（六原语保证前端零改动）并记录
- 宣称"完成"之前必须有证据：跑过的命令输出、截图、勾完的清单

## 协作模式（2026-09-21 定，覆盖上节中与之冲突的默认动作）

- **主会话作业**：不用子代理（coder/explore Agent），一切改动在主会话内完成，防注意力分散
- **不主动打包**：不主动跑 `pnpm tauri build`/出 exe；用户自己用 `build-release.bat` 或 dev 模式运行，明确要求出包时才构建
- **测试跟着修改走**：只测与本次修改直接相关的链路，不跑全量回归矩阵；关键页面抽查即可。例：改导出就测"写出→读回→数据一致"，不碰无关页面
- **探针克制**：只有现象无法从代码解释时才造测量工具，且先止损再深挖；纯文件/纯逻辑改动不造探针
- 分支纪律不变：dev 记录与合并、main 发布、新功能开 feature 分支、用户验收后才合 dev

## 本次不做（v1.x 后置项，出现即越界）

付印 PDF 导出、周视图、AI 边栏批注、色标命名、切换浮层快捷标色、稿库预备色标、设置项搜索、隐私窗口追踪、账号页。
